export enum CashflowType {
  INCOME = 'income',
  OUT = 'out',
  BOTH = 'both',
}

export enum CashflowTip {
  CASHFLOW = 'cashflow',
  ORDER = 'order',
  DEBT = 'debt',
}

export enum CashflowDirection {
  INCOME = 'Приход',
  EXPENSE = 'Расход',
}
