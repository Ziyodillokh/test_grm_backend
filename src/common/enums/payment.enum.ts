export enum PaymentMethod {
  CASH = 'cash',
  CARD = 'card',
  TERMINAL = 'terminal',
  MIXED = 'mixed',
  DEBT = 'debt',
}

export enum PayrollStatus {
  SENT = 'sent',
  REJECTED = 'rejected',
  IN_PROGRESS = 'in_progress',
  ACCEPTED = 'accepted',
}
