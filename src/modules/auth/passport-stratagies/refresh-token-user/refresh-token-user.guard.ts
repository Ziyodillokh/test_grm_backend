/**
 * Backward-compatible re-export.
 * @deprecated Import from `../../guards/jwt-refresh.guard` instead.
 */
export { JwtRefreshGuard as RefreshTokenUserGuard } from '../../guards/jwt-refresh.guard';
