/**
 * Backward-compatible re-export.
 * @deprecated Import from `../strategies/jwt-access.strategy` or `../constants` instead.
 */
export { ACCESS_TOKEN_COOKIE as ACCESS_TOKEN_USER } from '../../constants';
export { JwtAccessStrategy as AccessTokenUserStrategy } from '../../strategies/jwt-access.strategy';
